export enum SanitizeRole {
  ADMINISTRATOR = 'Administrador',
  COMMON = 'Colaborador',
  COORDINATOR = 'Coordenador',
  GUEST = 'Convidado',
}

export enum Role {
  ADMINISTRATOR = 'ADMINISTRATOR',
  COMMON = 'COMMON',
  COORDINATOR = 'COORDINATOR',
  GUEST = 'GUEST',
}

export enum RoleEmployeeForm {
  ADMINISTRATOR = 'ADMINISTRATOR',
  COMMON = 'COMMON',
  COORDINATOR = 'COORDINATOR',
  GUEST = 'GUEST',
}

export enum SanitizeStatusAccept {
  'pending' = 'Pendente',
  'approved' = 'Aceito',
  'rejected' = 'Negado',
}
